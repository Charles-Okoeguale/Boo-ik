import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles(() => ({
    container1: {
        width: '30%', 
        paddingTop: '0px',
        display: 'flex', 
        flexDirection: 'column', 
        height: '100%',
        alignItems: 'center',
    }
  
}));